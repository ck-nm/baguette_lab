<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>U info</h2>
      <%= render 'info', user: current_user %>
      <td><%= link_to 'Edit', edit_user_path(@user), class: "btn btn-sm btn-subtle edit_user_#{@user.id}" %></td>

      <h2 class="mt-3">New post</h2>
      <%= render 'public/posts/form', post: Post.new %>
    </div>
  </div>
</div>
